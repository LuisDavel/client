import { Story, Meta } from '@storybook/react/types-6-0';
import PhotoProfile from '.';

export default {
  title: 'PhotoProfile',
  component: PhotoProfile
} as Meta;

export const Default: Story = () => <PhotoProfile />;
